"use strict";
var members = ['azam', 'tooba', 'muzzzi', 'hasan', 'aisha'];
var message = 'Kal Kiski Birthday Hai:';
for (let i = 0; i < members.length; i++) {
    console.log(message + members[i]);
}
